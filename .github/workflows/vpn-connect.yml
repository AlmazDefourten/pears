name: GitHub Action

on:
  push:
    branches:
      - dev

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: deploy
        run: |
          export GOROOT=/home/u_studpr/goinst/go
          export GOPATH=$HOME/go
          export PATH=$GOPATH/bin:$GOROOT/bin:$PATH    
          source ~/.bashrc
          screen -ls | grep -o 'myapp' | sed 's/.$//' | xargs -I % sh -c 'screen -S % -X quit'
          cd ~/app/pears/api
          git pull --no-edit
          go build
          RUNNER_TRACKING_ID="" && screen -dmS myapp go run .
          screen -ls